package Basic_Codes;

import org.openqa.selenium.chrome.ChromeDriver;

import org.openqa.selenium.edge.EdgeDriver;

import org.openqa.selenium.firefox.FirefoxDriver;

public class Launching_Different_Browsers_Internally {

	public static void main(String[] args) {

		ChromeDriver A = new ChromeDriver();  // Launching the Chrome_Browser
		
		EdgeDriver B = new EdgeDriver();      // Launching the Edge_Browser
		
		FirefoxDriver C = new FirefoxDriver();  // Launching the Fire_fox Browser
		
		
		
	//	Note : If we want to Launch the Browser that specific Browser should present in our machine.
		
	}

}
